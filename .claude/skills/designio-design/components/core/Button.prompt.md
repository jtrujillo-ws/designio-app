Botón de acción; 'arco' (gradiente J1→J7) se reserva para el CTA que avanza el loop.
```jsx
<Button variant="arco">Aprobar gate G6</Button>
<Button variant="secondary" size="sm">Ver pantalla</Button>
```
Variantes: primary (tinta), arco (gradiente firma), secondary (outline), ghost, danger.